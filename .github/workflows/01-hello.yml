# .github/workflows/01-hello.yml

name: 01. Hello GitHub Actions
run-name: "[01] ${{ github.actor }}의 실행"
on:
  workflow_dispatch

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: 인사 출력
        run: echo "Hello, GitHub Actions!"
